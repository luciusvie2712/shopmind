# @shopmind/contracts

Purpose: Public framework-agnostic API contracts shared by ShopMind web and API.

Strategy: Manual TypeScript contracts for the MVP.

This package contains public API types only. It must not contain backend implementation, Prisma models, or NestJS decorated DTO classes.
